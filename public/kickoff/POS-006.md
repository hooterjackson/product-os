Cite POS-006 in your first message.

# POS-006 — Group D coverage: cluster 301 unattributed commits into items

product-os · doing · 240 min · gate none
Freshness: last audit 2026-08-19 (today) · 301 commits unattributed then

## What this is

Group D is a to-do list, not an error log. At the default window it is **301** commits that no item claims, against **29** items — roughly 10% of the portfolio modelled, with the missing 90% concentrated in `gimbal-bench`.

## Where it stands

No evidence recorded. It cannot be closed until something can be clicked.
Next, from the last handoff: one sentence from him; then create the items and re-measure.

## Already ruled out — read before proposing anything

- **R-058 · A coverage number that silently caps**
  `audit.py`'s commit query used `per_page=100` with no pagination. For `gimbal-bench` it returned **exactly 100** commits and reported that as the answer; the true count in the same window …
- **R-037 · Proving mute machinery offline**
  M6's first-hour P1 was invisible to
- **R-044 · Seeding or auditing from a local clone**
  $ git rev-list --count HEAD..origin/main # in the local clone 0 $ gh api .../compare/90ff98d...main --jq .ahead_by 54
- **R-049 · Publishing `gimbal-bench` today**
  **Authorized by the owner and blocked on one narrow, sufficient ground:** a capture names a real third party's personal email address in full, beside an incident on that person's tailnet …

## Work here, not in product-os

This item names no repo. Find out where the work lives before starting.

## Chats already working on this

- claude-code · work-laptop · Product-OS slice 1a-minus seed
  RESUME — 0 day(s) old, this machine, and nothing has landed in product-os since
  $ cd ~/Claude/product-os && claude -r c064d4d7-2187-41fd-9971-e5cc0ae0efca
- claude-code · work-laptop · BOOTSTRAP.md setup plan
  RESUME — 0 day(s) old, this machine, and nothing has landed in product-os since
  $ cd ~/Claude/product-os && claude -r 24ecfcb2-3fec-41bb-bc6e-898eb808df63

## Rules

- Nothing is done without evidence — a commit SHA, a file path, or a
  dated note. If you cannot produce one, it is not done. Say so.
- Say "I couldn't look", never "no changes". An empty result and an
  unreachable repo are different facts.
- Do not write his decided fields: impact, confidence, effort_minutes,
  cost_usd, unblocks, pin, gate, project, parked/dropped, or the
  evidence rule. Propose instead.
- The score is a label, not a verdict. Disagree once, with a reason,
  then do what he says.
