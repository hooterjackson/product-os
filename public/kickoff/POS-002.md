Cite POS-002 in your first message.

# POS-002 — Point PROJECT-STATE.md at product-os

product-os · doing · 25 min · gate none
Freshness: last audit 2026-08-19 (today) · 322 commits unattributed then

## What this is

**The single most likely way this repo fails is by becoming state file number nine** in a portfolio that already has eight, each confidently disagreeing with the others. `PROJECT-STATE.md` is the incumbent. It opens by telling every agent on every machine to read it first, and its header says 2026-07-31 against a remote that moved on until 2026-08-17 — nineteen days and a whole Doc 4a series later.

## Where it stands

Evidence on file: a482376.
Next, from the last handoff: push `a482376` from `~/Claude/engineered-lighting`, then flip to done.

## Already ruled out — read before proposing anything

- **R-051 · Trusting a header date over the git history under it**
  `PROJECT-STATE.md` listed two prompts as pending. **Both had already shipped**, before the file listing them as pending was committed. The file eventually corrected itself:

## Work here, not in product-os

Repo: `engineered-lighting-site`, `product-os`. product-os TRACKS the work; it does not host it. Make the change there and come back only to write the handoff.

## Chats already working on this

- claude-code · work-laptop · Product-OS slice 1a-minus seed
  RESUME — 0 day(s) old, this machine, and nothing has landed in product-os since
  $ cd ~/Claude/product-os && claude -r c064d4d7-2187-41fd-9971-e5cc0ae0efca
- claude-code · work-laptop · BOOTSTRAP.md setup plan
  RESUME — 0 day(s) old, this machine, and nothing has landed in product-os since
  $ cd ~/Claude/product-os && claude -r 24ecfcb2-3fec-41bb-bc6e-898eb808df63

## Rules

- Nothing is done without evidence — a commit SHA, a file path, or a
  dated note. If you cannot produce one, it is not done. Say so.
- Say "I couldn't look", never "no changes". An empty result and an
  unreachable repo are different facts.
- Do not write his decided fields: impact, confidence, effort_minutes,
  cost_usd, unblocks, pin, gate, project, parked/dropped, or the
  evidence rule. Propose instead.
- The score is a label, not a verdict. Disagree once, with a reason,
  then do what he says.
