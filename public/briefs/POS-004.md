# POS-004 · apply.py: distinguish who decided, not just what changed

`product-os` · done · lane infra · gate none

> ## ⚠ CLOSED ON MY JUDGEMENT, NOT CONFIRMED
>
> A machine decided this was finished. Marcelo has not said so.
> The evidence below is what I found; nobody has agreed it is enough.
>
> Confirm in a sentence: `apply.py --decided POS-004=status:done --said "..."`

**Freshness:** last audit 2026-08-19 · 153 commits unattributed then

## Where this stands

Score 5.3 · leverage 0 · 90 min.

Evidence on file: `tools/apply.py`, `tests/test_regressions.py`, `plugin/skills/audit/SKILL.md`

## What's next

Marcelo uses the loop for real. Every friction that surfaces is a real

## Already ruled out

Read these before proposing an approach:

- **R-059 · Believing the done-without-evidence guard stops a determined agent** — _matched on apply, authority, guard, truth_
  `apply.py` refuses `status: done` unless `evidence_found` existed **before the run**, so that evidence discovered by an audit is read by a person before it closes anything. The intent is right. The implementation measures the wrong thing.
- **R-074 · A proposal mechanism that outlived the fields it protected** — _matched on apply, authority, decided, proposal_
  **`state/proposals/` existed to protect the decided score inputs, and those are now deleted. After the cut the human-authority set is five fields** — `evidence`, `gate`, `machine_affinity`, `project`, and the `parked`/`dropped` statuses — plus item creation itself. Three mechanisms would then compete over those five, which is two too many: recommendations already carry "here is work you might want," and drafts already carry "here is the plumbing I drafted," with a word-level diff and a satisfiability refusal.
- **R-072 · A backlog the system wrote for itself** — _matched on authority, proposal_
  **The system generated ~30 items from the repos, then asked Marcelo to ratify the scores on work he had never chosen. `PROP-0001` was never answered, and the silence was the finding.** The authority model guarded `impact`, `confidence`, `unblocks` and the rest — but **creating an item was not guarded at all**, and creating one is a larger act than scoring it. A proposal asking "are these numbers right?" cannot be answered by someone whose actual objection is "I did not ask for any of this."
- **R-035 · Restoring the fault ring from flash whenever flash has a copy** — _matched on guard_
  `mirrorRestore()` returns immediately **unless layer one came back empty**.

## Decisions in force

- **DEC-202** (M2) — Marcelo orders the backlog; the tool stops computing an order

---

_Generated by `tools/brief.py` on every build. Never hand-edit: `build/` is rebuilt wholesale._
