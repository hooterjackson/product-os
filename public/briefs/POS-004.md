# POS-004 · apply.py: distinguish who decided, not just what changed

`product-os` · done · lane infra · gate none

> ## ⚠ CLOSED ON MY JUDGEMENT, NOT CONFIRMED
>
> A machine decided this was finished. Marcelo has not said so.
> The evidence below is what I found; nobody has agreed it is enough.
>
> Confirm in a sentence: `apply.py --decided POS-004=status:done --said "..."`

**Freshness:** last audit 2026-08-19 (today) · 153 commits unattributed then

## Where this stands

Score 5.3 · leverage 0 · 90 min.

Evidence on file: `tools/apply.py`, `tests/test_regressions.py`, `plugin/skills/audit/SKILL.md`

## What's next

Marcelo uses the loop for real. Every friction that surfaces is a real

## Already ruled out

Read these before proposing an approach:

- **R-059 · Believing the done-without-evidence guard stops a determined agent** — _matched on apply, authority, guard, truth_
  `apply.py` refuses `status: done` unless `evidence_found` existed **before the run**, so that evidence discovered by an audit is read by a person before it closes anything. The intent is right. The implementation measures the wrong thing.
- **R-035 · Restoring the fault ring from flash whenever flash has a copy** — _matched on guard_
  `mirrorRestore()` returns immediately **unless layer one came back empty**.
- **R-044 · Seeding or auditing from a local clone** — _matched on origin_
  $ git rev-list --count HEAD..origin/main      # in the local clone 0 $ gh api .../compare/90ff98d...main --jq .ahead_by 54
- **R-048 · Path-level guards for a field-level authority rule** — _matched on authority_
  A `PreToolUse` guard sees **a file**, not a field. Human-authority and agent-authority keys live in the same frontmatter block, so no path rule can tell which key moved. `validate.py`'s diff against a base ref is the only layer structurally capable of enforcing it.

## Decisions in force

**No ruling is recorded against this item.** It is parented to nothing, which means nobody has decided how it should go.

---

_Generated by `tools/brief.py` on every build. Never hand-edit: `build/` is rebuilt wholesale._
