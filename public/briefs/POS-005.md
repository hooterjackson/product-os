# POS-005 · Thread indexer: bind chats to items, metadata only

`product-os` · done · lane infra · gate none

> ## ⚠ CLOSED ON MY JUDGEMENT, NOT CONFIRMED
>
> A machine decided this was finished. Marcelo has not said so.
> The evidence below is what I found; nobody has agreed it is enough.
>
> Confirm in a sentence: `apply.py --decided POS-005=status:done --said "..."`

**Freshness:** last audit 2026-08-19 (today) · 153 commits unattributed then

## Where this stands

Score 3.0 · leverage 0 · 480 min.

Evidence on file: `tools/index.py`, `state/threads/by-machine/work-laptop.json`, `tools/validate.py`

## What's next

Marcelo uses the loop. Anchors accumulate on their own.

## Already ruled out

Read these before proposing an approach:

- **R-057 · Treating an item ID as unique across seed generations** — _matched on attachment, indexer, threads_
  The thread indexer's first run bound a real transcript to `Q-004`. It was wrong. That transcript's `Q-004` sits in this line:
- **R-060 · Believing `git commit --amend` removes an address from a public repo** — _matched on metadata_
  `validate.py`'s disclosure screen scans **file contents**. On a public repo, **author and committer emails are public too** — served by the API for every commit — and the screen never looked at them.
- **R-064 · Two agents writing one working tree** — _matched on sharding_
  The multi-machine design makes **cross-machine** conflict structurally impossible: `state/threads/by-machine/<id>.json` is written by exactly one machine's indexer, so two machines syncing the same day touch different files. **Nothing was designed for two agents on one machine, in one working tree.** That gap produced a false-alarm cascade in a single afternoon:

## Decisions in force

**No ruling is recorded against this item.** It is parented to nothing, which means nobody has decided how it should go.

---

_Generated by `tools/brief.py` on every build. Never hand-edit: `build/` is rebuilt wholesale._
