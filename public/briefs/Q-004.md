# Q-004 · Where do 300 liveness taps a second come from?

`gimbal-bench` · next · lane None · gate none

**Freshness:** last audit 2026-08-19 (today) · 301 commits unattributed then

## Where this stands

Score 3.0 · leverage 0 · 120 min.

**No evidence recorded yet.** This item cannot be closed until something can be clicked.

## What's next

**No handoff recorded.** Nobody has written down where this was left, so the next session starts from the item body.

## Already ruled out

Read these before proposing an approach:

- **R-036 · Quieting the firmware's logging first when chasing the serial flood** — _matched on 57, browser, d14, liveness, serial-flood_
  Quieting the firmware first would hide the browser bug under a lower volume rather than fixing it.
- **R-025 · Listen-only mode on the CANable `candleLight` tap** — _matched on tap_
  **It has no working listen-only mode** — the repo verified this directly: `L` is unimplemented and silent mode is doubly broken.
- **R-026 · Reading a pre-2026-08-13 wire capture without asking whether the tap was open** — _matched on tap_
  An open tap changes three things at once: TX succeeds, the error counters drain (*"CAN error counters fall only on a successful transmission"*), and canonical SAFE becomes reachable on a bus that could never otherwise produce it — measured at **within 3 s of opening the peer, evaporating within 2 s of closing it**.
- **R-029 · Replacing the dead-man with a hub-sourced liveness timer** — _matched on liveness_
  Over Zigbee a brief dropout takes **seconds** to recover, not milliseconds, so a 400 ms timer would trip constantly on a healthy system. Loosening it to seconds means seconds of uncommanded motion before anything reacts — a weaker net wearing the same name.

## Decisions in force

- **DEC-003** (D3) — Transport is Zigbee, C6 as router, light included
- **DEC-015** (D15) — Zigbee stays parked until the fault ring and the drills are done

---

_Generated by `tools/brief.py` on every build. Never hand-edit: `build/` is rebuilt wholesale._
